# Environment Variables Template
# Copy this file to .env.local and fill in your values

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="Machinery Vision"

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
API_SECRET_KEY=your-secret-key-here

# Database (if using)
DATABASE_URL=your-database-url-here

# Authentication (if using)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here

# Third-party Services
# Google Maps API (for location services)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Image Upload Service (if using)
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Email Service (if using)
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password

# Analytics (if using)
NEXT_PUBLIC_GA_TRACKING_ID=your-google-analytics-id

# Feature Flags
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_CHAT=false
# Supabase configuration
# Do NOT commit real values to version control. Use .env.local in development and Vercel Project Settings in production.
SUPABASE_URL=""
SUPABASE_ANON_KEY=""

# Optional: Service role key (server-only). Never expose to the browser.
# Not required for this app; manage in Vercel env if you use server-side admin tasks.
# SUPABASE_SERVICE_ROLE_KEY=""